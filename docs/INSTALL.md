# Установка

```bash
git clone git@github.com:BroknDwarf/vpn-manager.git
cd vpn-manager
sudo bash install.sh
```

Проверка:

```bash
vpn version
vpn status
vpn doctor
systemctl status vpn-manager-monitor.timer --no-pager
```
