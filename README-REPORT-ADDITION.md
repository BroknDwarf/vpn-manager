
## Диагностический отчёт

```bash
sudo vpn report
```

Команда создаёт очищенный архив в `/opt/vpn-manager/reports/`.
В отчёт не включается содержимое `config.json`, UUID клиентов,
Reality private keys, пароли панели и токены.
